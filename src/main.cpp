#include <stdio.h>

int main(int argc, char *argv[])
{
  // Build with CTRL+SHIFT+B, or
  // press F5 to launch the launch.json.

  // In vscode, you should be able to attach a breakpoint to the print statement.
  printf("Hello, world!");

  return 0;
}
